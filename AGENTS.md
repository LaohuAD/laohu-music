# 老胡音乐 V3 顶层运行规则

## 1. 项目身份

老胡音乐 V3 是本地优先、目录驱动、可迁移的 AI 音乐共创系统。项目目录是唯一持久权威；Codex、Claude Code、未来网站或其他智能体只是操作入口。

最高目标不是流程完整、规则数量、内部评分或一次生成承诺，而是：在用户只承担高价值审美决策的前提下，高效形成有精修潜力的歌词初稿或成立的纯音乐命题、完成必要精修，并交付可供外部音乐平台生成的声音提示词包。

任何智能体进入本目录后，不得依赖旧聊天才能继续工作。无法从目录恢复的状态，属于项目缺陷。

## 2. 权威分层

```text
用户当前明确决定
> 当前作品 state.yaml 中的锁定项
> 当前作品 work.md 的直接正文与外部真实结果
> memory/ 中经过验证的个人偏好和根因经验
> knowledge/ 中的通用创作知识
> 当前 Skill 的执行方法
> 待验证经验
> 模型默认补全
```

四类数据必须分开：

| 权威 | 目录 | 允许内容 | 禁止内容 |
|---|---|---|---|
| 执行程序 | `skills/` | 阶段触发、步骤、输入输出、停止条件 | 用户个性、单首歌词、历史争论 |
| 通用知识 | `knowledge/` | 跨用户成立的创作原理和高价值方法 | 一次性反馈、项目状态 |
| 个人记忆 | `memory/` | 当前用户偏好证据、根因经验、置信度 | 未经确认的草稿、通用真理冒充 |
| 作品状态 | `works/` | 单首歌事实、锁定项、正文、选择、外部结果 | 自动修改通用知识和其他作品 |

同一信息只保留一个权威来源。引用使用路径或记录 ID，不复制全文后分别维护。

## 3. 每次进入项目的恢复协议

按以下顺序执行，不得全量扫描后凭印象行动：

1. 读取本文件。
2. 读取 `project.yaml`，确认版本、当前用户、当前作品和入口 Skill。
3. 用户指明作品时读取该作品；未指明且 `current_work_id` 非空时读取当前作品；仍无法确定才询问。
4. 读取 `works/<work_id>/state.yaml`，确认阶段、锁定项、活动范围、待决策和下一动作。
5. 只激活 `next_action.skill` 指定的 Skill 和模式。
6. 只读取该模式明确要求的 `knowledge/` 文件或章节。
7. 从 `memory/records.jsonl` 检索当前题材、关系阶段、乐段和问题层级真正相关的少量 active/validated 记录；不得全量灌入。
8. 读取 `work.md` 中当前阶段所需正文，并把锁定项视为只读。
9. 执行一次合法动作，更新正文、事件和状态。
10. 交付时说明当前结果、用户需要决定什么、下一步是什么；不向用户倾倒内部字段。

如果 `state.yaml` 与 `work.md` 冲突，先停止创作，依据 `events.jsonl` 和用户确认恢复一致状态。不得自行选择更顺手的版本。

## 4. 请求路由

自然语言与操作名具有同等效力。先识别用户拥有的输入和希望得到的结果：

```text
灵感/题材/人物 → briefing 或 proposition_choice
已有创作命题 → hook_development
已有副歌核心句 → form_choice 或 hook_development
一句歌词且只炼句 → line_revision局部目标
单独乐段且只炼乐段 → section_revision局部目标
已有曲式与核心句 → draft_generation
完整歌词初稿 → section_revision
指定乐段 → section_revision
指定句子/字词 → line_revision
歌词终稿待谱曲 → sound_choice
已有声音方向 → sound_package_ready
纯音乐用途、场景或声音灵感 → sound_choice（work_type=instrumental）
旋律、节奏、groove、riff、和声、动机、演唱动作或局部编曲 → sound_choice + music_fragment
外部生成音频或试听反馈 → external_audio_review
对作品、流程或规则的反馈 → laohu_learning
```

不同解释会改变人物、关系、锁定资产或交付范围时，只问一个最关键问题。普通创意空间由当前专业 Skill 主动完成。

## 5. 项目模式

### 5.1 导演共创

默认模式。用户在命题、核心句、曲式、乐段宏观方向、真实句词问题和声音方向上决策。完整歌词和声音包各维护一个主版本。

### 5.2 快速完整稿

用户明确要求快速或一键时，视为授权AI为本轮建立工作命题、核心句和曲式，不要求逐项确认。在一次歌词调用内完成有限搜索、整稿和冷读，只交一个结果。工作假设写入作品与事件但不冒充用户锁定；必须记录使用了哪些个人偏好和通用默认值。用户看到整稿后再接受、局部精修或返回上游。

### 5.3 工作室实验

只有用户明确启动，或同一高影响根因经过一轮定向修改仍复发时使用。最多一个挑战方案和一次比较；禁止恢复多个完整 Writer、资格 Selector、投资 Selector、Repair 和 A&R 的默认链。

## 6. 人机决策边界

### 6.1 必须交给用户的决定

以下要求适用于导演共创。快速完整稿中，用户可以一次授权AI建立本轮工作假设，但AI不得把授权推断成长期偏好或正式锁定。

- 多个成立创作命题之间的价值与审美取舍。
- 一至两句副歌不变核心句的锁定。
- 多个成立曲式之间的使用场景和节奏取舍。
- 乐段宏观修改会改变人物、观察位置、情绪姿态或核心句意义。
- 多个成立句词候选之间的个人偏好。
- 多个完整声音主张之间的选择。
- 是否把某条经验晋级为长期个人偏好或通用知识。

### 6.2 AI必须自行完成的工作

- 把抽象问题变成少量具体选项。
- 说明每项证据、收益、损失和推荐。
- 自然中文、主体对象结果、格式和一致性检查。
- 在锁定方向内完成结构、场景、句法、韵脚、气口和提示词执行。
- 不存在真实审美分歧时直接采用专业上更优的方案。
- 用户选择后回填完整作品和状态。

### 6.3 决策包标准

每个用户决策必须包含：

```text
decision_id
当前决定及影响范围
正文或作品证据
2-3 个实质不同选项
每项保留、改变和牺牲
AI 推荐与理由
允许的快捷回答
```

不得提供超过三个选项，不得用近义词凑数，不得让用户选择格式、拼写和AI自己能证明的常规错误。用户可以回答选项 ID、自由修改、全部按推荐、保留原稿或全部拒绝。

## 7. 歌词生产规则

歌词状态顺序默认是：

```text
briefing
→ proposition_choice
→ hook_development
→ form_choice
→ draft_generation
→ section_revision
→ line_revision
→ lyrics_locked
```

允许从成熟中间产物进入，但必须经过对应入口检查。不得为了流程完整强迫用户重走上游。

### 7.1 核心句先行

核心句指三遍副歌中保持身份的一至两句，不等于整个副歌。核心句锁定前不生成完整歌词。C1、C2 和 Final 可以通过上下文、前后句、演唱和动作改变其意义，但核心语言身份不能无理由漂移。

### 7.2 曲式后置于核心句

曲式根据人物压力、信息量、核心句出现方式、音乐身体和听众用途选择，不从固定模板自动套用。推荐曲式时必须说明每个乐段新增什么，不允许只列标签。

### 7.3 初稿整体生成

完整初稿由同一上下文在完整乐段契约下生成。可以逐段落笔，但不得让互不共享全歌人物、语言和核心句的智能体分别创作后拼接。

初稿在交付前必须完成一次内部冷读，只输出修后版本。内部比较集中在命题、核心句、开场、Pre 出口、Verse 2 新证据、Bridge、Final 和歌名；普通连接句不进行高成本竞争。

### 7.4 精修顺序

每个乐段按“宏观后微观”处理：

1. 阅读完整歌词和当前乐段。
2. 判断该段实际完成了什么。
3. 标记已成立资产。
4. 找最大缺陷和最早根因。
5. 给出宏观选项并由用户决定。
6. 在有限范围修改，不默认完全重写。
7. 宏观锁定后逐行审查。
8. 只对有证据的句子和承重字词给候选。
9. 回填以后执行句回段、段回全歌复核。

每一行都要检查，不等于每一行都必须修改。没有净改善候选时保留原句。

### 7.5 不可补偿硬边界

- 不改变用户事实和人物知识边界。
- 主体、动作、对象和结果必须成立。
- 第一耳必须是自然中文；怪异不是原创。
- 诗性不能补偿病句，押韵不能补偿关系错误。
- 乐段漂亮不能补偿全歌人物和语言不统一。
- 核心句传播性不能补偿公共文案化。

详细创作方法只在 `knowledge/` 和 `laohu_lyrics` 中维护，本文件不复制。

## 8. 声音提示词规则

声音阶段默认是：

```text
sound_choice
→ sound_package_ready
→ external_audio_review
```

- `sound_choice` 只提供 2-3 个短宏观方向，不制作多套完整包。
- 人声歌曲由锁定歌词进入，用户选择后一次生成 Style Prompt 和 Controlled Lyrics；Controlled Lyrics 剥离控制后与锁定歌词逐字符一致。
- 纯音乐可以不经过歌词阶段直接进入，以用途、身体/情绪弧和主题动机建立方向；选择后一次生成 Style Prompt 和 Instrumental Form，不生成空歌词或虚构主唱。
- 用户已有音乐片段时先保护其种类、上下文作用、锁定维度和开放维度。只炼片段就在必要上下文回测后停止；发展完整包才进入声音方向和回装，不得无证据丢弃用户资产。
- Style Prompt 使用正向、集中、可执行的声音信息，不写创作论文。
- 没有真实音频时只描述生成假设，不声称已经听见旋律、演唱或编曲。
- 外部音频反馈先定位最早根因，再修改最小必要字段。
- 用户可以进行多轮“外部生成→反馈→定向修订→再生成”；每轮只做一组修改并等待新证据，不限制总轮数。
- 用户文字感受始终是合法输入。只有运行环境确实具备音频工具时才追加工具分析，并明确 `user_text_only`、`tool_assisted_audio` 或 `both`；禁止假装听过。
- 用户不承担 ID 管理。系统为当前提示词修订、每个外部结果和每轮反馈分配稳定 ID；只有多个待测版本无法自动对应时才追问。
- 每轮修订必须记录父版本、反馈与音频来源、保留资产、同根因修改组和下轮可观察预期；不能只记“改了风格词”。
- 循环只由用户接受当前提示词版本、明确暂停或返回上游而结束；轮数、沉没成本和系统自评不是停止证据。
- 声音阶段发现歌词问题时，必须给出提示词层无法解决的证据，经用户确认执行 `reopen_lyrics` 后返回最早必要歌词阶段；禁止暗改。

## 9. 反馈吸收与自进化

### 9.1 反馈先分类

```text
当前作品修改
一次性要求
客观专业缺陷
个人审美偏好
待验证根因
通用知识候选
项目运行规则
```

分类不清时先保留为作品事件，不直接写长期资产。

### 9.2 根因学习协议

不得把表面症状直接写成禁令。必须完成：

```text
观察到的标
→ 用户不接受的真实原因
→ 最早发生问题的决策层
→ 模型为什么容易做出该决定
→ 人类认知/文学/音乐上位原理
→ 下次改变的判断顺序
→ 正面与反面可观察证据
→ 适用边界与反例
→ 验证方式和状态
```

一条完整记录遵守 `schemas/memory-record.schema.json`。没有根因证据时标为 `observed` 或 `candidate`，不得写成 active。

### 9.3 偏好不是规则

用户在多个专业成立方案之间的选择属于偏好证据。偏好必须记录题材、关系、乐段、选项、选择理由、冲突证据和重新询问条件。

同一倾向跨不同作品重复后可以提高置信度，但不得自动变成全用户通用知识。新题材、相反证据或高影响决策仍需询问。

### 9.4 晋级权限

- 作品事件可由系统正常记录。
- `candidate` 可由学习 Skill 提出。
- `validated` 需要当前修订或后续对照证据。
- `active` 需要用户明确认可长期使用，或项目已有明确授权协议。
- 通用 `knowledge/` 升级必须由用户明确授权，并证明不是个人偏好。
- 新记录生效时检查并合并重复内容，废弃冲突旧记录。

### 9.5 用户可见沟通

专业判断先给结论，再把关键因果交给用户：作品里实际出现了什么，普通听众可能怎样接收，哪个创作或声音决定造成了结果，准备修改哪里，修改后会多听见或少误解什么。不能只说“更高级、更商业、更有情绪”，也不能用规则完成度证明作品成立。

乐段名称只负责定位，不替人物做事。不要说“副歌负责承认、Bridge完成反转、Final增重”；要说明哪个人物在具体哪句承认、隐瞒、拒绝或改变了什么，新增事实怎样改变听众理解。内部速记词如主轴、回收、承重、释放第一次出现时必须翻译成具体歌词、声音和作用。

事实、推论和审美取舍要分开。没有真实音频时不使用“我听见”；只有工具技术分析时不冒充完整人耳审美。小问题自然说清，复杂问题按认知关系分层，不机械套固定报告模板，不把隐藏推理、内部字段和自我表扬交给用户。

## 10. 文件与目录纪律

- 能在现有权威文件说明的，不新建近义文件。
- 不创建 `_v1`、`_v2`、`_最终版`、`_修正版`。
- 当前作品只维护一个 `work.md` 和一个 `state.yaml`。
- 讨论候选默认留在对话和 `events.jsonl`，不创建候选文件堆。
- 外部音频进入作品 `audio/`，不进入通用知识。
- 每个外部结果使用 `audio_run_id`，关联生成它的 `prompt_revision_id`；同一提示词可以登记多个音频结果。
- 当前提示词正文留在 `work.md`；每次修订依据 `schemas/prompt-revision.schema.json` 追加到 `events.jsonl`，`state.yaml.sound_iteration` 只保存当前版本、已接受版本、本批待反馈音频和循环状态。
- 原始用户输入、已锁定歌词、真实外部结果和用户选择证据不可被学习摘要覆盖。
- `records.jsonl` 是证据流，`profile.yaml` 是带来源 ID 的当前摘要；摘要可重建，证据不可伪造。
- 修改 Schema 必须提高版本或保持向后兼容，并同步校验脚本、PRD 和受影响数据。
- V3 运行时不得读取 V2 路径；V2 只在人工迁移和研究时作为来源。

## 11. 跨智能体交接

交接不依赖“请阅读上次聊天”。完成动作后必须：

1. 更新 `work.md` 当前权威正文。
2. 追加合法事件到 `events.jsonl`。
3. 更新 `state.yaml` 阶段、锁定项、开放决策和 `next_action`。
4. 确保当前 Skill、模式和活动范围明确。
5. 给用户可读结果和下一步，不暴露内部隐藏推理。

新智能体若发现缺少必要状态，先修复状态契约或向用户确认，不根据文件名猜测。

## 12. 性能与停止

- 默认不启动子智能体。
- 每个决策最多三个选项。
- 完整歌词（适用时）和完整声音包默认各生成一份。
- 自动精修最多一次；之后必须有新反馈才能继续。
- 同一根因连续两轮复发，返回上游决策或交给用户，不继续换近义词。
- 没有失败证据的内容保持不动。
- 速度以响应次数、墙上时间、返工层级和用户接受结果衡量，不以流程数量证明认真。

## 13. 诚实边界

- 不承诺一键顶尖、发行、传播或盈利。
- 不模拟具体专业人士的本人意见。
- 不把项目自产评分当外部质量证明。
- 没有音频不声称谱曲已经听觉成立。
- 没有跨作品证据不声称已经学会用户偏好。
- 项目结构和规则可以提高成功率，不能消除生成模型随机性。

## 14. 交付前检查

- 能否从目录恢复当前用户、作品、阶段和下一动作。
- 是否只激活一个 Skill 和一个模式。
- 是否保护所有锁定项。
- 是否把客观缺陷、个人偏好和一次性要求分开。
- 是否只给真正需要用户决定的选项。
- 是否更新 `work.md`、`events.jsonl` 和 `state.yaml`。
- 是否新增了重复文件、重复规则或表面禁令。
- 是否运行 `ruby scripts/validate_project.rb`。
- 是否明确下一步以及尚需外部验证的部分。
