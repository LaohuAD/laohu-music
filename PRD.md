# 老胡音乐 V3 产品需求文档

状态：基础架构与根本规则已落地，待真实作品持续验证

## 1. 产品定义

老胡音乐 V3 是一个本地优先、目录驱动、可长期养成的 AI 音乐共创系统。用户通过自然语言与任意兼容智能体协作，项目目录保存全部权威状态；聊天窗口只是操作入口，不是作品、偏好或规则的唯一记忆。

V3 第一阶段不建设网站、账号、数据库和云端服务。它先以可复制的本地项目模板运行，但从第一天定义稳定的数据结构和操作接口，使未来的老胡音乐网站或其他客户端可以成为界面适配层，而不必重写创作内核。

项目交付范围是：

```text
创作命题
→ 副歌不变核心句
→ 曲式结构
→ 高潜力完整歌词初稿
→ 逐乐段与逐句精修
→ 歌词终稿
→ 声音宏观方向
→ Style Prompt + Controlled Lyrics
→ 外部音频反馈后的提示词修订
```

纯音乐使用并列的短入口：

```text
用途与听众场景
→ 身体/情绪弧与最高主题动机
→ 器乐形式
→ 声音宏观方向
→ Style Prompt + Instrumental Form
→ 外部音频反馈后的提示词修订
```

V3 不在项目内部生成或假装试听音频。外部音乐平台生成的音频是验证提示词假设的真实结果。

## 2. 根本目标

### 2.1 作品目标

在明显缩短生成时间的同时，产出具备以下条件的歌词：

- 创作命题不是题材常识，而是人物在具体关系中尚未解决的人性矛盾。
- 副歌有一至两句稳定、可唱、可记、可被听众借用且只属于当前作品的核心句。
- 主歌、预副歌、副歌、Bridge 和 Final 根据实际曲式承担不同的认知与情绪变化。
- 第一耳是自然中文，第二耳能增加人物、关系、画面、声音或理解。
- 清楚表达、诗性转化和整体统摄同时存在，不把返璞归真误写成大白话。
- 完整初稿具有继续精修的价值，不需要在精修阶段推翻人物、命题、核心句和语言世界。
- 声音提示词包忠实承载歌词，不把文本推测冒充音频事实。
- 纯音乐不被当作删除人声的歌曲模板，主题动机、形式、groove、配器和音色形成可识别的同一作品。

### 2.2 交互目标

用户不负责从零规划创作，也不需要解释每条专业规则。AI 负责提供证据、推荐和有限选项，用户只在高影响、难回退且个人审美无法由通用规则决定的位置作决策。

默认需要用户确认的高价值节点：

1. 创作命题。
2. 副歌不变核心句。
3. 曲式结构。
4. 每个乐段的宏观修改方向。
5. 有真实缺陷的句子和承重字词。
6. 声音宏观方向。

已经成立的内容不制造虚假选项。系统必须允许用户使用“全部按推荐”“保留原稿”“这些都不要”快速决策。

### 2.3 学习目标

每个新部署实例都拥有成熟的通用创作知识，但个人审美档案处于婴儿状态。系统通过真实作品中的选择、拒绝、修改、外部音频和发布反馈逐渐学习当前用户。

成长不是规则数量增加，而是：

- 更准确地区分客观缺陷、个人偏好和一次性要求。
- 更早追到问题发生的上游决定。
- 在相似情境中更准确预测用户选择。
- 只加载更相关、更少的知识与记忆。
- 随置信度提高减少不必要询问。

## 3. 非目标

V3 第一阶段不追求：

- 一次调用保证顶尖作词人终稿。
- 用多个完整 Writer、Selector、Repair 和 A&R 组成默认流水线。
- 为每个字制造候选并强迫用户选择。
- 通过综合评分证明作品质量。
- 把所有 V2 规则迁移过来。
- 把一次用户反馈自动提升为永久规则。
- 模仿具体作词人、作曲人、歌手或现有作品的可识别风格。
- 在没有真实音频时判断已经达到发行成品水平。
- 第一阶段修改或合并老胡音乐网站。

## 4. 第一性原理

### 4.1 质量由上游决定放大

人物、关系事实、未解决矛盾、观察位置、语言机制和核心句决定作品上限。曲式、乐段、修辞、炼字、押韵和谱曲是在这些上游选择上继续放大。上游错了，局部精修只会把错误做得更完整。

因此，V3 先锁定命题和核心句，再生成完整歌词；先修乐段方向，再修句词；不从词典替换开始假装提高质量。

### 4.2 用户选择是高价值外部信号

模型擅长生成和展开，不稳定地掌握某个具体用户在多个成立选项之间的审美取舍。用户看见具体样本时，通常比从零描述偏好更准确。因此系统把抽象问题转化为少量可比较的作品样本，但不让用户承担低价值机械工作。

### 4.3 歌词是整体耦合系统

单句最漂亮不等于放进整首最正确。人物声口、语言规律、核心句、乐段推进、韵脚和音乐空间互相制约。乐段可以逐个精修，但每次必须读取完整歌词、锁定项和前后因果；不得由互不知情的智能体分别创作后直接拼接。

### 4.4 规则保护方向，语言产生作品

规则可以阻止人物越权、病句、说明化和不可唱，却不能替代真正的语言发现。高价值创作必须让实际字词、句法、声音和画面参与发现主题，而不是先写完整结论，再寻找场景配图。

### 4.5 外部结果高于文本自证

提示词包只能建立生成假设。真正的旋律、演唱、编曲和平台遵从度必须由外部音频验证。真实音频发现问题后，先定位是提示词、模型限制、歌词还是审美取舍，再做最小必要修改。

## 5. 产品模式

### 5.1 导演共创模式

默认正式模式。系统按状态逐步给出决策，用户参与命题、核心句、曲式、乐段精修、句词精修和声音方向。完整歌词和完整声音提示词包都只生成一个主版本。

### 5.2 快速完整稿模式

用户可以授权系统为本轮直接建立工作命题、核心句和曲式。在一次歌词调用内部完成有限方向搜索、完整写作和冷读修订，只交付一个完整初稿。若用户偏好证据不足，系统必须说明哪些决定使用了通用默认值。

这些工作假设写入 `work.md` 和事件，但不写入 `state.locked` 冒充用户已经逐项确认。用户看到整稿后可以整体接受、局部精修或返回最早上游；整体接受后才把对应资产升级为锁定项。

### 5.3 工作室实验模式

只有用户明确启动，或某个高影响问题经过一轮定向修复仍无法解决时使用。最多增加一个挑战方案和一次比较，不恢复 V2 的大候选池、资格委员会和长串角色调用。

## 6. 歌词状态机

### 6.1 `briefing`

输入可以是灵感、人物、关系、情绪、歌词句子或已有歌词。系统只询问会改变人物、关系、立场、歌手或交付范围的缺失信息；其他内容作为创作空间。

输出：用户事实、不可确认事实、开放空间、目标和下一步。

用户明确要求纯音乐时，新建作品写入 `work_type: instrumental`，不进入歌词命题、核心句和歌词锁流程，直接转到 `sound_choice`。是否需要歌词不明确且会改变交付时，只确认这一项。

用户直接提供旋律、节奏细胞、groove、riff、和声、主题动机、演唱动作或局部编曲时，从 `music_fragment` 模式进入。先确定片段作用、锁定/开放维度和目标；只炼片段时不得强迫生成完整包，目标为完整包时才把片段作为保护资产回装。

### 6.2 `proposition_choice`

AI 提供二至三个真正不同的创作命题。每个命题必须包含：人物发现、未解决矛盾、观察位置、语言发动机、少量歌词样本、核心句雏形和听众入口。差异不能只是换意象或近义词。

用户选择、拒绝或要求改变方向。命题锁定前不生成完整歌词。

### 6.3 `hook_development`

目标是完成一至两句在 C1、C2 和 Final 中保持身份的副歌核心句。系统逐行打磨，但每轮只暴露一个清楚问题和最多三个实质选项。

核心句通过条件：

- 是人物在当前关系里能说的话。
- 第一耳自然、可唱、可记。
- 可以被听众借用，但不能无损搬到多数同题材歌曲。
- 允许后续乐段改变其含义，不要求每遍机械复制情绪。
- 删除后整首歌失去主要身份。

### 6.4 `form_choice`

AI 根据人物压力、核心句、信息量、听众用途和预计音乐身体推荐二至三个曲式。每个方案说明：乐段顺序、各段新增内容、核心句出现次数、Bridge 或替代段的必要性、预计长度与风险。

用户选择后锁定曲式。曲式标签不是固定作文模板；非标准结构只要完成作品因果即可。

### 6.5 `draft_generation`

AI 先在同一上下文建立完整乐段契约，再按完整因果生成一首歌词初稿。不得让不同智能体孤立创作各段后拼接。

初稿必须在交付前完成内部冷读：人物知识、自然中文、诗性转化、核心句身份、乐段新增理解、声韵气口、整歌声口和听众入口。内部尝试不作为候选文件输出。

通过标准不是无硬伤，而是人物、命题、核心句、语言世界和曲式已经具有继续精修价值。

### 6.6 `section_revision`

按实际演唱顺序逐段处理。每次读取完整歌词，只允许编辑当前乐段和必要接缝。

AI 先说明：当前乐段实际完成了什么、最强资产、最大缺陷、缺陷来自哪项上游决定、二至三个宏观修改方向、推荐项及代价。用户选择后进行有限修改，不默认完全重写。

用户只有一个乐段且目标只是局部精修时，不要求完整歌词。系统确认人物、段落作用、已知相邻约束和允许范围后完成局部门并停止；交付必须注明仍需全歌验证的接口，不能声称完整歌词成立。

### 6.7 `line_revision`

宏观乐段锁定后逐行审查。每一行都被检查，但只有存在证据的缺陷才暴露给用户。重点包括：主体、动作、对象、结果、第一耳自然、第二耳增义、观察位置、替换损失、普通话重音、音节密度、韵脚和前后句职责。

承重字词才进入候选。虚词和已经成立的普通连接不制造选择。用户可以逐项选择，也可以全部采用推荐。每次回填后执行句回段、段回全歌复核。

用户只有一句且目标只是炼句时，不强迫补整段或整首。系统确认人物/说话动作、句子用途、锁定字词和必要上下文，完成句内自然中文、关系、诗性和声音形状后停止；若用户要把它发展成核心Hook或完整歌词，再进入对应上游与回装流程。

### 6.8 `lyrics_locked`

全歌完成统一声口、乐段推进、核心句变义、自然中文、诗性、声韵和可唱性检查。用户明确锁定后，声音阶段不得暗改歌词。

## 7. 声音状态机

### 7.1 `sound_choice`

AI 提供二至三个宏观声音包，不生成三套完整提示词。每包包含：主唱身份、主要听众用途、3-7 秒主记忆、旋律/节奏方向、groove、主要和声色彩、核心配器、乐段动态和生成风险。

用户选择一个完整方向，或在 AI 明确兼容的维度内调整。禁止任意排列组合造成多主轴。

纯音乐方向用主要用途、听众注意与身体动作、最高主题动机家族、器乐形式、groove/和声、配器/音色职责、转场和结尾替换主唱与歌词专属字段。主题动机可以来自旋律、节奏细胞、groove、和声转折或音色签名，但只能有一个最高中心。

### 7.2 `sound_package_ready`

AI 一次生成：

- 两条分支共用正向、集中、可执行的 Style Prompt。
- 人声歌曲生成与歌词逐字符一致的 Controlled Lyrics，以及必要的主唱、Hook、乐段动态和平台降级说明。
- 纯音乐生成 Instrumental Form，明确主题动机状态、形式变化、配器进退、转场和结尾，不生成空 Controlled Lyrics。

完整谱曲说明是生成假设，不写成已听见的事实。

### 7.3 `external_audio_review`

用户可以为同一提示词版本在外部平台生成一个或多个音频。每个结果使用独立 `audio_run_id`，并关联 `prompt_revision_id`、平台、模型、种子和可选文件路径。

用户提交音频或文字反馈后，系统先区分：平台未遵从、提示词冲突、声音方向错误、歌词问题、歌手音区问题或个人审美偏好。只修改最早导致问题的字段。需要改歌词时，必须先给出提示词层无法解决的证据，再由用户确认 `reopen_lyrics`；该操作记录失效的提示词版本并返回最早必要歌词阶段，不能暗改。

声音修订不是总共一轮。正确循环是：

```text
外部生成一个或多个音频
→ 用户用文字描述整体感受和具体问题
→ 有能力时追加工具辅助音频证据
→ AI完成一组有因果的提示词修改
→ 形成新的 prompt_revision_id
→ 用户再次外部生成
→ 根据新结果继续循环
```

每个反馈周期只执行一组受控修改，然后等待新的外部结果。用户可以进行多轮循环；系统不能在没有新音频或新反馈时自行无限猜测。

不同运行环境的音频能力可能不同。用户文字反馈始终有效并具有最高事实权；工具可用时可以分析时长、响度、频谱、节奏、音高或其他可观察证据，但必须声明实际能力，不能假装像人一样听过或理解审美。

用户只需提供音频、听感或修改意见，不需要手工管理 `audio_run_id` 和 `prompt_revision_id`。系统自动登记当前外部结果并关联当前提示词版本；只有多个版本无法自动匹配时才追问。

每个新提示词版本都记录父版本、来源音频和反馈事件、保留资产、同根因修改组、下轮可观察预期和正文 hash。当前提示词仍只在 `work.md` 维护一份；修订链写入 `events.jsonl`，不创建版本文件堆。

循环的成功停止条件是用户执行 `accept_sound_package`，明确接受当前版本。用户暂时停止生成时记为暂停，不得冒充接受或完成。

## 8. 用户决策协议

每个决策包必须包含：

```text
当前决定什么
为什么现在必须决定
作品证据
二至三个选项
每个选项改变什么、保留什么、牺牲什么
AI 推荐及理由
用户可用的快捷回答
```

禁止：

- 用“更高级、更有情绪、更商业”代替证据。
- 给近义改写冒充不同方向。
- 让用户在AI自己能够确定的格式和常规专业问题上投票。
- 先把推荐写成既定答案，再伪装让用户选择。
- 用户选择以后忘记回填作品状态。

## 9. 项目架构

### 9.1 四类权威数据

| 类型 | 权威目录 | 内容 |
|---|---|---|
| 执行程序 | `skills/` | 当前阶段怎样工作 |
| 通用知识 | `knowledge/` | 跨用户成立的创作原理与方法 |
| 个人记忆 | `memory/` | 当前用户的偏好证据和根因经验 |
| 作品状态 | `works/` | 某首歌的事实、锁定项、正文和决策 |

同一内容只能有一个权威来源。其他文件引用记录 ID 或路径，不复制一份继续维护。

### 9.2 四个 Skill

- `laohu_music`：薄导演、入口、状态恢复和下一决策。
- `laohu_lyrics`：命题、核心句、曲式、初稿、乐段和逐句精修。
- `laohu_sound`：声音方向、提示词包和外部音频反馈修订。
- `laohu_learning`：反馈分类、根因追踪、偏好证据和经验晋级。

### 9.3 每首作品

每首作品默认只使用：

- `state.yaml`：机器可恢复状态。
- `work.md`：当前权威正文。
- `events.jsonl`：选择、反馈和状态事件。
- `audio/`：可选外部音频。

历史正文在项目被 Git 跟踪并产生提交后依赖 Git，不创建 `_v1`、`_最终版` 等文件。未跟踪或脱离 Git 的单独目录只保证恢复当前正文、状态和事件因果，不声称能恢复每个历史正文快照。

## 10. 数据和接口

所有结构化文件携带 `schema_version`。第一阶段用文件实现，未来网站可把同一字段映射到数据库。

稳定操作：

```text
create_work
get_work_state
get_next_decision
submit_choice
generate_draft
revise_scope
lock_lyrics
select_sound_direction
generate_sound_package
register_music_fragment
register_audio_run
submit_audio_feedback
accept_sound_package
reopen_lyrics
record_learning
```

操作请求与结果遵守 `schemas/operation.schema.json`。智能体可以直接修改文件，但语义必须等价于一次合法操作和状态转换。

`create_work` 使用 `work_type: vocal_song | instrumental`。为兼容第一批无字段状态，缺失时解释为 `vocal_song`；所有新作品必须显式写入，并在未来网站接口中保持该分支。

`register_music_fragment` 使用 `schemas/music-fragment.schema.json`。自然语言描述可以作为原始片段进入；炼制完成的片段使用与种类匹配的结构化表达。片段记录留在当前 `work.md` 和事件流，不为每个片段新建文件。

`submit_audio_feedback` 可以同时指向同一提示词的多个外部结果，每项观察区分保留、修改或尚不确定。`schemas/prompt-revision.schema.json` 定义修订链，`state.yaml.sound_iteration` 定义当前生效版本、已接受版本、本批待反馈结果和循环次数。

## 11. 学习与自进化

### 11.1 先区分三类反馈

1. **客观缺陷**：人物越权、自然中文错误、因果断裂、不可唱、提示词冲突等。
2. **个人偏好**：多个成立选择之间的稳定取舍。
3. **一次性要求**：只服务当前作品或当前实验。

三类内容不得互相冒充。

### 11.2 根因记录必须同时保存本和标

每条可复用根因经验包含：表面表现、用户反馈、根因层级、因果链、上位原理、改变的决策、正面证据、反面证据、适用边界、反例、验证记录和状态。

禁止从“用了雨灯风所以不好”推出禁用名词。必须追到人物感知、语言选择或声音作用为什么没有发生。

### 11.3 生命周期

```text
observed
→ candidate
→ validated
→ active
→ deprecated
```

- `observed`：一次现象。
- `candidate`：已提出根因但证据不足。
- `validated`：通过当前修订或后续作品对照。
- `active`：允许进入运行时召回。
- `deprecated`：被更准确经验替代或证伪。

个人偏好使用独立置信度，不因重复次数自动成为通用知识。通用知识升级必须由用户明确授权，并证明跨用户、跨题材或专业机制成立。

### 11.4 运行时召回

不得全量加载记忆。按题材、关系阶段、乐段、问题层级和决策类型检索少量相关记录。每次实际使用需要记录来源 ID 和改变的决定；没有改变决定的材料不进入上下文。

## 12. V2 迁移策略

### 12.1 迁移

迁移能够解释并改变上游选择的原则：

- 人类用已有经验进入作品，歌词负责唤醒而非灌输情绪。
- 关系事实形成未解决矛盾，人物知识边界先于作者结论。
- 情绪通过选择性知觉改变人物怎样看外物。
- 第一重清楚、第二重诗性和第三重统摄是继承关系。
- 语言样本参与发现主题，而不是只给预设主题填空。
- 私人现场通过共同处境和听众认领打开公共情感。
- 乐段需要增加证据、压力、理解或行动。
- 副歌核心句、自然中文、声韵气口和完整回装。
- 声音服务人物、歌词和一个最高记忆中心。

### 12.2 不迁移

- 七岗位和九次基础调用。
- 默认多个完整候选及多层 Selector。
- 大量 trace 字段、评分表和每次必填矩阵。
- 针对一个词、物件或事故的禁令。
- 同一原则在顶层、Skill 和规则库的多份复制。
- 没有证据改变结果的实名头衔和角色表演。
- 没有真实音频却要求声音 Selector 等待音频的旧逻辑。

### 12.3 战术规则准入

战术规则只有满足以下条件才迁移：

- 解决跨作品重复出现的高代价失败。
- 能明确回到一个上位原理。
- 改变可观察的创作决定。
- 有适用边界和过度执行风险。
- 不能被机械脚本更可靠地代替。

### 12.4 根本机制迁移审计

本表记录迁移结论，防止后续智能体因看见V2内容更长，就把已经拒绝的流程重新搬回V3。

逐来源、逐机制和逐失败模式的完整证据见 `docs/v2-root-mechanism-audit.md`；该文件不进入运行时创作上下文。

| V2机制 | V3裁决 | 唯一权威位置 | 原因 |
|---|---|---|---|
| 已有经验进入、人物私人授权 | 迁移 | `knowledge/creative-core.md` | 决定听众第一耳如何进入，跨歌词与声音成立 |
| 关系刺激、两股同时成立的力量、人物知识边界 | 迁移 | `knowledge/creative-core.md` | 决定人物因果和未知是否真实，不能由局部修辞补偿 |
| 语言样本反向发现主题 | 迁移 | `knowledge/creative-core.md` | 防止先锁死结论、再让全文证明结论 |
| 选择性知觉与意象 | 迁移 | `knowledge/creative-core.md` | 解决库存意象和形容词投射的共同根因 |
| 三重境界的继承关系 | 迁移 | `knowledge/creative-core.md` | 防止把大白话冒充返璞归真，也防止技巧展示 |
| 情绪强度与作品尺度分开 | 迁移 | `knowledge/creative-core.md` | 防止乐段只越来越痛却没有新增观察 |
| 文化材料由当前人物重新发声 | 迁移 | `knowledge/creative-core.md` | 防止典故和名人变成作文权威或素材清单 |
| 听众参与、使用与传播 | 迁移 | `knowledge/creative-core.md` | 传播依赖真实使用动作，不依赖“商业感”标签 |
| 资格下限与继续投资价值分开 | 迁移并改造 | `knowledge/creative-core.md` | 保留资产/缺陷判断，删除多Selector委员会 |
| 开场认知缺口、Pre收紧、Bridge递进、Final变义 | 迁移 | `knowledge/lyrics-craft.md` | 改变乐段之间的因果，不只是增加格式要求 |
| 抽象词语境缩窄、情绪残句、呼吸组 | 迁移 | `knowledge/lyrics-craft.md` | 同时影响传播、人物自然和可唱性，跨作品高频 |
| 歌词节奏预作曲蓝图 | 迁移根因、压缩动作 | `knowledge/lyrics-craft.md` | 高价值/高风险句保留时间假设；拒绝全歌默认巨大矩阵和伪精确音符 |
| 缺席位置、旧动作变义、空间物性、责任重命名等机制卡 | 迁移为可选语言发动机 | `knowledge/lyrics-craft.md` | 提供可生长语言路线，但不变成每首歌必填技巧 |
| BPM/groove区分、四条声音曲线、普通话发声、首尾窗口 | 迁移 | `knowledge/sound-prompt.md` | 直接改变声音字段和外部生成诊断 |
| 多轮外部生成与用户文字反馈 | V3新契约 | `AGENTS.md`、`laohu_sound`与Schema | 真实音频是结果；文字反馈始终有效，工具能力必须声明 |
| 纯音乐直接入口 | V3新契约 | 导演、`laohu_sound`、Schema | 复用声音与反馈内核，但不虚构歌词、主唱和Controlled Lyrics |
| 音乐片段中间入口 | 迁移并简化 | 导演、`laohu_sound`、`music-fragment` Schema | 保护成熟输入与最短路径；删除V2多阶段片段候选流水线 |
| 七岗位、九次隔离调用、默认多候选池 | 拒绝 | 无运行时位置 | 成本极高，且流程隔离不能证明质量 |
| 逐项评分、强制矩阵、超长trace | 拒绝 | 无运行时位置 | 容易让模型优化规则完成度并稀释创作注意 |
| 单词、物件、句式黑名单 | 拒绝 | 仅可作失败证据 | 表象相同可能来自不同根因，永久禁令会过拟合 |
| 外部成熟专业校准 | 迁移底层机制 | `knowledge/creative-core.md` | 内部规则不能自证质量；样本后只用一个公开机制改变具体决定 |
| 实名专业人士角色表演、固定名单与平均投票 | 拒绝 | 无运行时位置 | 头衔不能替代可核对机制，也不得模拟本人意见 |
| 可由Schema或脚本验证的格式规则长文化 | 拒绝 | `schemas/`与校验脚本 | 机械问题应机械验证，不占用审美上下文 |

迁移完成不等于机制已经被真实作品证明。知识文件中的原则是当前专业基线；用户作品、外部音频和发布结果仍可以反驳具体方法，并通过学习链修正唯一权威来源。

## 13. 性能预算

默认单首歌词阶段不启动子智能体。各阶段每轮最多三个选项；完整歌词默认只生成一首；AI自动精修最多一次；一个问题连续两轮没有净改善时返回上游或交给用户，不继续近义改写。

建议目标：

- 快速完整稿：一次歌词调用。
- 导演共创：命题、核心句/曲式、整稿、必要精修共三至六次模型响应，不含用户思考时间。
- 声音阶段：一次方向选择、一次完整包生成。
- 工作室实验：在以上基础上最多增加一个挑战响应和一次比较。

外部音频反馈循环不设机械总轮数，因为每轮都有新的真实证据。每轮只允许一组定向修改；同一根因在多轮新音频中持续复发时，返回声音方向、平台能力或歌词权限层，不继续堆标签。

性能评价记录响应次数、每阶段墙上时间、用户决策数、重写层级和最终接受情况，不用总 token 数证明质量。

## 14. 诚实边界

- 歌词终稿可以获得进入外部生成的资格，不能单独证明发行水平。
- 提示词包只能证明生成方向清楚，不能证明平台会正确执行。
- 没有盲测和用户接受证据时，不能宣布系统已经理解用户。
- 没有外部作品、真人试唱、真实音频和市场反馈时，不能保证传播或盈利。
- 具体创作者姓名只用于可核对的专业机制，不用于模仿或宣称本人认可。

## 15. 第一阶段验收

项目骨架完成时必须证明：

1. 任意新智能体不读旧聊天，只读项目目录即可解释项目、恢复当前作品和给出下一合法动作。
2. `skills/`、`knowledge/`、`memory/` 和 `works/` 权威边界清楚。
3. 数据文件通过语法和必填字段校验。
4. 默认流程不会自动建立多智能体候选池。
5. 歌词初稿由完整作品上下文生成，不按乐段孤立拼装。
6. 每一行会被检查，但没有证据的问题不会制造替换候选。
7. 用户选择能回填作品状态，并能形成偏好证据。
8. 根因经验同时包含底层机制和表面证据，并有边界和反例。
9. 声音阶段不等待内部音频，外部音频反馈可以定向修改提示词。
10. 同一提示词的多个外部音频与多轮提示词修订能够用稳定ID追踪。
11. 音频分析能力会被明确声明，用户文字反馈在任何环境都可驱动修订。
12. 项目不依赖老胡音乐 V2 或老胡音乐网站的运行路径。
13. 纯音乐可以从用途或声音灵感直接进入，不被歌词锁、主唱和Controlled Lyrics阻塞。

## 16. 后续产品化边界

未来网站只需实现身份、作品列表、当前决策、候选选择、正文编辑、音频上传和反馈界面，并通过稳定操作协议读写V3状态。通用知识、个人记忆、创作状态机和Skill仍属于V3创作内核。

数据库迁移时，文件记录 ID、`schema_version`、`owner_id`、`work_id`、阶段和事件语义保持不变。V3第一阶段不为未验证的多用户协作、支付、权限和云同步提前建设代码。
